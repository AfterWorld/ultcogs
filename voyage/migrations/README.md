# Voyage Migrations

Phase 1 only creates player foundation tables:

- `characters`
- `cooldowns`
- `schema_migrations`

Future phases should add contracts, crews, voyages, event/audit tables, and
scheduler state here as separate numbered SQL migrations.
